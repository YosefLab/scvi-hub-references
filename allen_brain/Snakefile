envvars:
    "HF_API_TOKEN"

rule all:
    input:
        "config.json"
    log:
        "logs/stderr.log",
        "logs/stdout.log",
    conda:
        "env.yaml"
    threads: 16
    script:
        "main.py"
